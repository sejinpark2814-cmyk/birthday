<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>세진이가 보내는 메시지 💌</title>
    <style>
        body { text-align: center; background-color: #ffebee; font-family: 'Nanum Gothic', sans-serif; padding-top: 100px; }
        h1 { color: #d32f2f; }
        .btn-container { position: relative; height: 300px; }
        button { padding: 15px 30px; font-size: 18px; cursor: pointer; border-radius: 10px; border: none; transition: 0.3s; }
        #yesBtn { background-color: #ff4081; color: white; }
        #noBtn { background-color: #9e9e9e; color: white; position: absolute; }
    </style>
</head>
<body>
    <h1>박세진 ❤ 여자친구이름</h1>
    <p>이번 주말에 나랑 데이트할래?</p>
    
    <div class="btn-container">
        <button id="yesBtn" onclick="alert('역시 그럴 줄 알았어! 🥰 이따 연락할게!')">응!!!</button>
        <button id="noBtn" onmouseover="moveButton()">아니오</button>
    </div>

    <script>
        function moveButton() {
            const x = Math.random() * (window.innerWidth - 100);
            const y = Math.random() * (window.innerHeight - 100);
            const noBtn = document.getElementById('noBtn');
            noBtn.style.left = x + 'px';
            noBtn.style.top = y + 'px';
        }
    </script>
</body>
</html>